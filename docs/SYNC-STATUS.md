# Repository sync status

**Source of truth:** `ISA und A+I Projekt/GitHub/-ISAAI-Automatic-Report-Documentation`  
**Remote:** https://github.com/pollo60/-ISAAI-Automatic-Report-Documentation.git

## Layout change (2026-05-30)

Canonical documentation moved under `docs/Documentation/` (previously `Documentation/` at repo root on older clones).

## Verify locally

```bash
./scripts/verify-repo-sync.sh
```

## Related copies

| Location | Notes |
|----------|-------|
| `A + I/-ISAAI-Automatic-Report-Documentation/` | Older git clone; prefer this workspace |
| `Projektdateien/` | Local working artifacts; merged into `docs/` where unique |

Last updated: 2026-05-30 — initial consolidation commit pending push.
